[project]
name = "{{ names.snake_case }}"
{% if include_stamp %}sdk_version = "{{ sdk_version }}"
{% endif %}
[entrypoint]
file_path = "{{ xex_path }}"
out_directory_path = "{{ out_directory_path }}"
includes = []
